# Kinescope_Linker

## Функционал

* Добавляет кнопки с прямой ссылкой на трансляцию.
* Автоматически отслеживает появление новых трансляций на странице и добавляет к ним кнопки без перезагрузки.
* Подсвечивает карточки (`div`) в зависимости от состояния трансляции:
  * 🟩 **Зелёным** — если трансляция **уже завершилась**.
  * 🟧 **Оранжевым** — если трансляция **идёт в данный момент**.
  * 🟦 **Синим** — если трансляция состоится **сегодня, но ещё не началась**.
* Раз в минуту обновляет подсветку, чтобы цвета соответствовали актуальному времени.
* Реагирует на изменения DOM (добавление/удаление блоков) с помощью `MutationObserver`.

## Установка для: Google Chrome, Яндекс Браузер
1. Создаем папку "Kinescope_Linker" и перемещаем в нее файлы "content.js" и "manifest.json";
2. Переходим по ссылке: `chrome://extensions/`;
3. В правом верхнем углу есть ползунок "Режим разработчика". Если он отключен, то включаем его;
4. Появляется кнопка "Загрузить распакованное расширение". Нажимаем на нее и выбираем папку "Kinescope_Linker";
5. Готово!
